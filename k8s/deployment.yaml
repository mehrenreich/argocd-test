apiVersion: apps/v1
kind: Deployment
metadata:
  name: argocd-test-deployment
  labels:
    app: argocd-test
spec:
  replicas: 1
  selector:
    matchLabels:
      app: argocd-test
  template:
    metadata:
      labels:
        app: argocd-test
    spec:
      containers:
        - name: argocd-test
          image: mehrenre/argocd-test:1.0.0
